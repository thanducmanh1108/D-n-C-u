@model IEnumerable<baithi.Models.ncc>

@{
    ViewBag.Title = "Nhà cung cấp";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script src="~/Scripts/jquery-3.4.1.min.js"></script>

@if (TempData["AlertMessage"] != null)
{
    <div id="AlertBox" class="alert @TempData["AlertType"] hide">
        @TempData["AlertMessage"]
    </div>
}
<script>
    $("#AlertBox").removeClass("hide");
    $("#AlertBox").delay(3000).slideUp(500);
</script>

<p class="create" style="text-align:left">
    @Html.ActionLink("Thêm nhà cung cấp mới", "Create")
</p>


<table class="table">
    <tr>
        <th>
            Tên nhà cung cấp
        </th>
        <th>
            Số điện thoại
        </th>
        <th>
            Địa chỉ
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tenncc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sdt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.diachi)
            </td>
            <td class="custom-icon">
                @Html.ActionLink("Sửa", "Edit", new { id = item.IDncc }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.IDncc })
            </td>
        </tr>
    }

</table>
